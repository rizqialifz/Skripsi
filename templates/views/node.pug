extends ../layouts/default
include ../mixins/device

block content
	.page-header
		h1 Node
	.row
		.col-sm-6
			//- p= devices.results
			each node in sensornodes
				//- h3= node._id
				+sensornode(node)

		.col-sm-6


